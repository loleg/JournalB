{{ image rendition="thumbnail" }}
	<img src="{{ $image->src }}" width="{{ $image->width }}" height="{{ $image->height }}" alt="{{ $image->caption }} (Foto: {{ $image->photographer }})" />
{{ /image }} 
